import { prettierPluginLatex } from '@unified-latex/unified-latex-prettier';

export const options = {
  ...prettierPluginLatex.options,
};

export const defaultOptions = {
  ...prettierPluginLatex.defaultOptions,
};
